<!doctype html>

<html>
    <head>
        <meta charset = "utf-8">
        <title>{{pageHead}}</title> 
    </head>

    <body>
        {{!--HEADER--}}
        {{> header}}
        <p>{{upperIt bodyMssg}}</p>

        {{!--FOOTER--}}
        {{> footer}}
    </body>
</html>